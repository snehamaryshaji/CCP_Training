#include<iostream>
#define MAXCAP 100
using namespace std;

int main()
	{
	/*int capcurrent, usedcap;
	cout << "current usage " << endl;
	cin >> capcurrent;
	usedcap = MAXCAP - capcurrent;
	//if (-1)//except 0 everything is high if(0==0) 
	if (usedcap == 0)
	{
		cout << "memory is full" << endl;
		return 0;
	}
	cout << "memory is not full" << endl;
	return 0;
	*/
	int x = 10;
	int y = 20;
	printf("%d", x,y);//y will ignore
	printf("%d,%d", x);//y garbage value
	return 0;
	}